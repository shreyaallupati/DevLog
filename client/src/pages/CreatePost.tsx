// Protected Page

export const CreatePost = () => {
    return (
        <div style={{ padding: '2rem' }}>
        <h1>Create a New DevLog</h1>
        <p>Only logged-in users should see this!</p>
        </div>
    );
};